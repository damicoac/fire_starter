# Building Modules

To build a new community module:
1. Create a new `.go` file in `src/modules/community/`.
2. Ensure it implements the module interface.
3. Write an accompanying `_test.go` file.
4. Submit a Pull Request.